// The final lesson of the Variables Temple: 'const cannot change, let can evolve. Choose wisely based on your needs...'

// Create a variable `score` using `let` with initial value 0. Then reassign it to 100. Finally, log the score to the console.

// Declare score with let
// Then reassign it
let score = 0;
score = 100;
console.log(score);
