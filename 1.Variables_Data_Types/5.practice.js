// In the depths of the code, you discover the concept of nothingness. The Codex reveals: 'null represents intentional absence, undefined means not yet defined...'

// Create two variables: `emptyBox` set to `null`, and log `emptyBox` to the console.

// Declare emptyBox and log it
let emptyBox = null;

console.log(emptyBox);
