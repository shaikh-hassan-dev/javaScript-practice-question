// Numbers hold power in this realm. The Codex reveals: 'Count your steps, measure your progress...'

// Create a variable named `age` with the value 25 (as a number, not a string).

// Declare the age variable
let age = 25;
