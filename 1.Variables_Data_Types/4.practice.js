// At the crossroads of logic, two paths emerge: true and false. The Codex teaches: 'Every decision begins with a yes or no...'

// Create a variable named `isActive` with the boolean value `true`.

// Declare a boolean variable
let isActive = true;
